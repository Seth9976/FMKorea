package androidx.work;

public enum l {
    NOT_REQUIRED,
    CONNECTED,
    UNMETERED,
    NOT_ROAMING,
    METERED,
    TEMPORARILY_UNMETERED;

    private static final l[] b() [...] // Inlined contents
}

