package androidx.work.impl;

import java.util.concurrent.TimeUnit;

public abstract class z {
    private static final long a;

    static {
        z.a = TimeUnit.DAYS.toMillis(1L);
    }

    public static final long a() [...] // 潜在的解密器
}

